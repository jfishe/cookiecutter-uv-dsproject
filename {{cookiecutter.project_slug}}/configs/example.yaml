# Example experiment configuration for {{ cookiecutter.project_name }}.
# Usage: uv run python scripts/train_model.py -c configs/example.yaml -v

dataset: "features.parquet"   # file in data/processed/
target: "target"              # target column name

model_params:
  n_estimators: 100
  max_depth: 8
  random_state: 42

model_file: "model_v1.joblib"
metrics_file: "metrics_v1.json"
